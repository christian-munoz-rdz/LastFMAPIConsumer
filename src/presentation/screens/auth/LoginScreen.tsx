import { Typography } from "@mui/material"

const LoginScreen = () => {
    return (
        <Typography variant="h1" gutterBottom>
        Login
      </Typography>
    );
}

export default LoginScreen;