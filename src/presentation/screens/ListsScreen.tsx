import { Typography } from "@mui/material"

const ListsScreen = () => {
    return (
    <Typography variant="h1" gutterBottom>
        Listas
      </Typography>
    );
}

export default ListsScreen;