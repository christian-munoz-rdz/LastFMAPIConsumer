import { Typography } from "@mui/material"

const ProfileScreen = () => {
    return (
    <Typography variant="h1" gutterBottom>
        Profile
      </Typography>
    );
}

export default ProfileScreen;